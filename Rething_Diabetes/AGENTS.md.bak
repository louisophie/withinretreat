# Rething_Diabetes 翻譯工作備註

本目錄是 Gary Taubes《Rethinking Diabetes: What science reveals about diets, insulin, and successful treatments》的中文（正體／台灣）選譯工作區。

## 工作檔案
- `tmp.md` — 前言翻譯（含 Zettlr 段落註 `<!--00N-->`），已完成校對並可一併對照 `00.md`／`00.html`。
- `00.md` / `00.html` — 另一份前言／系列文件與其 HTML 匯出。
- `tmp.html` — 由 Zettlr/pandoc 匯出，移動端可用 `mobile.css`。
- `mobile.css` — 手機閱讀用的回應式字型，於 Zettlr 匯出設定中掛載。

## 體例
- 人名／機構：`中文名（原文）`，如 Elliott Joslin（艾略特·喬斯林）；期刊一律 `雜誌名（中文名）`，並善用譯注/原注。
- 一律正體中文；注意全形標點、`內`（非内）、`為`（非爲）、`得`（如「高得不成比例」）。
- 先看 `tmp.md` 既有用詞，保持術語一致（糖尿病「第二型／第一型」、碳水、生酮等）。

## 檢查要點
- 對照原文句意，避免誤譯；留意英文專名拼法（如 Harley 非 Harly、Virta Health）。
- 長句可在中文處以「；」或拆分短句確保通順。
- 讓使用者確認後才批量編輯。

## 其他提醒
- GitHub Pages 已啟動：`https://louisophie.github.io/withinretreat/`。
- 此檔案符合 opencode 慣例：於本目錄或其下啟動 `opencode` 時會自動載入本檔。